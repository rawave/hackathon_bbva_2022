version: "3.9"
services:
  nfc:
    image: "python:slim" # TODO: check image
    volumes:
      - ./nfc:/code/nfc
      - ./files:/code/files
    working_dir: /code/nfc
    entrypoint: ./run.sh
    ports:
      - 80:8000